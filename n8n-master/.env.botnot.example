# === n8n для BotNot (одна машина с бэкендом) ===
# Скопируйте в .env в корне n8n-master или задайте переменные перед запуском.

# Слушать на всех интерфейсах (для доступа с сервера)
N8N_HOST=0.0.0.0
N8N_PORT=5678
N8N_PROTOCOL=http

# Путь к данным n8n (workflows, credentials) — на сервере лучше задать отдельный каталог
# N8N_USER_FOLDER=./n8n-data

# Если бэкенд и n8n на одной машине без Docker — в workflow уже задано http://localhost:3000
# Если n8n в Docker, а бэкенд на хосте: в узле Config workflow укажите backendUrl: http://host.docker.internal:3000

# Базовый URL для webhook (если нужен полный URL в ответах)
# WEBHOOK_URL=http://localhost:5678/
