# Анализ сайта CLABX и предложения по улучшению

Цель: привести платформу к уровню лучших крипто-трейдинг приложений (Binance, Bybit, 3Commas, Cryptohopper, eToro) по качеству, надёжности и конкурентоспособности.

---

## 1. Текущие сильные стороны

| Область | Что уже хорошо |
|--------|-----------------|
| **Функционал** | Сигналы, бэктест (RSI + свечи), авто-трейдинг, копитрейдинг, соц. торговля (лидерборд), сканер, график (lightweight-charts + стакан), демо, PnL-калькулятор |
| **Авторизация** | Регистрация/вход, группы, ключи активации, подписка по времени, разграничение вкладок по группам |
| **Админка** | Dashboard, пользователи (поиск, продление/отмена подписки, смена логина/пароля, бан), группы, ключи, тарифы, прокси, логи, аналитика, торговый статус |
| **API** | REST + WebSocket для сигналов и пробоев, единый error handler, CORS |
| **UI** | Тёмная/светлая тема, единые CSS-переменные, тосты, навигация по вкладкам и URL (pushState/popState), хоткеи Ctrl+1–9 |
| **Интеграции** | OKX (свечи, ордера, баланс), Telegram (уведомления, бот для ключей), прокси для обхода блокировок |

---

## 2. Пробелы относительно лучших крипто-платформ

### 2.1 Безопасность и доверие

| Проблема | Рекомендация |
|----------|--------------|
| Нет восстановления пароля | Добавить «Забыли пароль?» → ссылка/код по email или через Telegram-бота (если email не собираем). |
| Нет 2FA (TOTP) | В профиле/настройках: «Двухфакторная аутентификация» (QR + код). Обязательно для вывода/API или опционально для входа. |
| Пароль только в localStorage | Токен уже в localStorage — нормально; убедиться, что ключи OKX не логируются и не отдаются фронту целиком (только статус подключения). |
| Нет явного «Безопасность» в профиле | Вкладка/блок: смена пароля, 2FA, активные сессии (опционально), история входов. |

### 2.2 UX и навигация

| Проблема | Рекомендация |
|----------|--------------|
| Нет React Router | Сейчас URL синхронизируется вручную (pushState). Для глубоких ссылок и SEO лучше внедрить React Router (или аналог) и единый роутер. |
| Мобильное меню | В шапке много пунктов — на узких экранах сделать бургер-меню и выдвижную навигацию. |
| Нет хлебных крошек | На длинных сценариях (Настройки → Подключения) добавить breadcrumbs. |
| Нет глобального поиска | Поиск по тикерам/страницам (как на биржах) — в шапке поле «Поиск пары/страницы». |
| Нет скелетонов загрузки | Вместо одного «Загрузка…» — скелетоны карточек/таблиц для Dashboard, Сигналы, Соц. торговля. |

### 2.3 Контент и информирование

| Проблема | Рекомендация |
|----------|--------------|
| Слабый SEO | В `index.html`: `meta name="description"`, `og:title`, `og:description`, `og:image` для шаринга. Отдельные title/description для ключевых страниц при переходе на роутер. |
| Нет онбординга | После первого входа — короткий тур (highlight ключевых блоков: Сигналы, График, Настройки OKX). Можно сохранять флаг «тур пройден» в профиле. |
| Риск-дисклеймер только в футере | Дублировать краткое предупреждение о рисках при первом входе в Авто-торговлю и Копитрейдинг (галочка «Понял риски»). |
| Нет статус-страницы | Страница или поддомен status.clabx.ru: доступность API, WebSocket, биржи. Улучшает доверие. |

### 2.4 Торговый опыт

| Проблема | Рекомендация |
|----------|--------------|
| График без TradingView | Сейчас lightweight-charts — достаточно для старта. Долгосрочно: встраивание TradingView Widget или полноценной интеграции для привычного UX. |
| Нет истории ордеров на графике | На графике отмечать точки входа/выхода по истории сделок пользователя (если есть `/orders`). |
| Сигналы без «причины» в списке | В ленте сигналов показывать краткую причину (RSI, пробой, MTF) и ссылку на разбор (например, модалка или переход на график). |
| Нет алертов по цене | Пользовательский алерт «уведомить при цене BTC > X» (хранить в БД, проверять по таймеру или при обновлении тикера). |
| Бэктест без экспорта | Экспорт результатов в CSV/PDF для отчётов и сравнения стратегий. |

### 2.5 Социальная торговля и копитрейдинг

| Проблема | Рекомендация |
|----------|--------------|
| Лидерборд без фильтров | Фильтры по периоду (7d / 30d / all), по паре, по просадке. |
| Нет рейтинга/верификации провайдеров | Бейдж «Верифицирован» (ручная или по объёму/возрасту аккаунта), рейтинг по подписчикам и отзывам (если добавите отзывы). |
| Нет статистики по парам у провайдера | В карточке провайдера: PnL по основным парам, drawdown по времени. |
| Копирование без лимитов риска | В настройках подписки: макс. доля на одну сделку, макс. дневная просадка, стоп-копирование при просадке > X%. |

### 2.6 Надёжность и масштабирование

| Проблема | Рекомендация |
|----------|--------------|
| Нет rate limiting на API | На критичные и публичные эндпоинты (login, /signals, /auth/register) — rate limit по IP и по userId (например express-rate-limit). |
| Нет централизованного логирования ошибок фронта | Sentry (или аналог) + источник (страница, действие) для быстрого разбора багов. |
| Один WebSocket на всё | Разделить каналы: сигналы, пробои, тикеры, уведомления — и подписываться только на нужные (меньше трафика и путаницы). |
| Нет health-check для зависимостей | `/api/health` расширить: статус БД, OKX (ping), очереди (если появятся). Для мониторинга и алертов. |

### 2.7 Мобильное и офлайн

| Проблема | Рекомендация |
|----------|--------------|
| Нет PWA | manifest.json + service worker: «Установить приложение», базовый офлайн-экран и кэш статики. |
| Нет адаптивных таблиц | На мобильных большие таблицы (сигналы, лидерборд) — карточки или горизонтальный скролл с закреплённой первой колонкой. |

---

## 3. Конкретные исправления в коде

### 3.1 Единый API-клиент с авторизацией

Сейчас часть страниц передаёт `headers: { Authorization: Bearer ${token} }` вручную, часть использует `fetch` без токена. Риск: забыть токен на новом эндпоинте.

**Предложение:** в `utils/api.ts` добавить опцию «всегда подставлять токен из контекста/хранилища»:

- Либо прокидывать `getToken: () => string | null` в конфиг api и в каждом запросе добавлять `Authorization`, если токен есть.
- Либо отдельный `authApi.get/post/...`, который читает токен из localStorage и добавляет в заголовки.

Так все вызовы к защищённым эндпоинтам будут единообразными и не забудут авторизацию.

### 3.2 Сигналы: единый источник запросов

`SignalFeed` использует прямой `fetch('/api/signals')` без токена. Если в будущем решите отдавать персональные/расширенные сигналы по токену — лучше сразу использовать общий api-клиент с опциональным токеном, чтобы не дублировать логику.

### 3.3 Мета-теги и SEO

В `index.html` добавить:

```html
<meta name="description" content="CLABX — торговые сигналы, авто-трейдинг и копитрейдинг для криптовалют. Подключение OKX, бэктест стратегий, социальная торговля." />
<meta property="og:title" content="CLABX 🚀 Crypto Trading Soft" />
<meta property="og:description" content="Торговые сигналы, авто-трейдинг и копитрейдинг для крипты. OKX, бэктест, лидерборд." />
<meta property="og:image" content="https://clabx.ru/logo.png" />
<meta property="og:type" content="website" />
```

При появлении роутинга по страницам — выставлять `document.title` и при необходимости обновлять `og:*` для шаринга.

### 3.4 Обработка 401 на основном API

В `utils/api.ts` при ответе 401 (или 403) можно:

- Очищать токен и редиректить на страницу входа (или вызывать колбэк `onUnauthorized`, по аналогии с админкой).
- Показывать тост «Сессия истекла, войдите снова».

Так пользователь не останется в интерфейсе с «битым» токеном.

### 3.5 Навигация на мобильных

В `App.tsx` для экранов с шириной &lt; 768px:

- Скрыть список пунктов навигации в шапке.
- Показать кнопку «бургер», по клику — выдвижная панель или выпадающее меню со всеми пунктами из `PAGES`.

Это уменьшит беспорядок в шапке и улучшит использование на телефонах.

---

## 4. Приоритизация улучшений

### Высокий приоритет (безопасность и стабильность)

1. **Rate limiting** на `/api/auth/login`, `/api/auth/register`, по возможности на `/api/signals`.
2. **Восстановление пароля** (через Telegram-бота или email, если появится).
3. **Единый api-клиент с авторизацией** и обработка 401 (очистка токена + редирект/тост).
4. **Мета-теги и базовый SEO** в `index.html` (и при роутинге — по страницам).

### Средний приоритет (UX и доверие)

5. **Мобильное меню** (бургер + выдвижная навигация).
6. **Скелетоны загрузки** для Dashboard, Сигналы, Соц. торговля.
7. **Риск-дисклеймер** при первом использовании Авто-торговли и Копитрейдинга.
8. **Расширенный /api/health** (БД, OKX, при необходимости WebSocket).

### Ниже по приоритету (конкурентные фичи)

9. **2FA (TOTP)** в профиле/настройках.
10. **Фильтры и период** в лидерборде соц. торговли.
11. **Лимиты риска** при подписке на копитрейдинг (макс. доля, макс. просадка).
12. **PWA**: manifest + service worker, кнопка «Установить приложение».
13. **Онбординг-тур** после первого входа.
14. **Алерты по цене** (уведомление при достижении цены).
15. **Экспорт бэктеста** в CSV/PDF.

---

## 5. Итог

Платформа уже покрывает сильный сценарий: сигналы, бэктест, авто-торговля, копитрейдинг, соц. торговля, админка и интеграция с OKX и Telegram. Чтобы уверенно конкурировать с лучшими крипто-трейдинг приложениями, стоит:

- **Укрепить безопасность:** rate limit, восстановление пароля, затем 2FA.
- **Унифицировать клиент и сессию:** один способ отправки токена и обработки 401.
- **Улучшить UX на мобильных и при загрузке:** бургер-меню, скелетоны, риск-дисклеймеры.
- **Усилить доверие:** SEO, статус-страница, расширенный health-check.
- **Поэтапно добавлять «биржевые» фичи:** фильтры лидерборда, лимиты риска в копитрейдинге, алерты по цене, экспорт бэктеста, при необходимости — интеграция с TradingView.

Реализацию удобнее вести по приоритетам выше: сначала высокий приоритет, затем средний, затем оставшиеся пункты по ресурсам и обратной связи пользователей.
