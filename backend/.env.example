# OKX API (Торговля)
# ВАЖНО: Не коммитьте .env в git! После утечки ключей — перевыпустите их на OKX.
OKX_API_KEY=
OKX_SECRET=
OKX_PASSPHRASE=

# OKX Testnet (демо-счёт): OKX_SANDBOX=1 — торговля на тестовом счёте
# OKX_SANDBOX=0

# Авто-трейдинг: исполнение ордеров через OKX. Без флага — только сигналы.
# Включить: AUTO_TRADING_EXECUTION_ENABLED=1 (осторожно: реальные деньги при OKX_SANDBOX=0)
# AUTO_TRADING_EXECUTION_ENABLED=0

# Прокси для запросов к OKX (обход Cloudflare). Список через запятую.
# PROXY_LIST=http://user:pass@ip:port,http://user:pass@ip2:port
PROXY_LIST=

PORT=3000
NODE_ENV=development
# LOG_LEVEL=debug|info|warn|error (default: info)

# База данных ордеров (SQLite). По умолчанию: data/cryptosignal.db относительно cwd.
# Папка data/ также используется для ml_model.json (онлайн ML).
# DATABASE_PATH=./data/cryptosignal.db

# ML: минимум примеров, после которого AI-фильтр (minAiProb) учитывается. По умолчанию 15.
# ML_MIN_SAMPLES_GATE=15

# Пароль админ-панели (по умолчанию из ТЗ). Для продакшена задайте свой.
# ADMIN_PASSWORD=your_secure_password

# Токен для API Telegram-бота (X-Bot-Token). Бот вызывает POST /api/bot/register-key, POST /api/bot/revoke-key, GET /api/bot/plans.
# BOT_WEBHOOK_SECRET=your_secret_string

# Токен бота от @BotFather — для запуска Telegram-бота (telegram-bot/).
# TELEGRAM_BOT_TOKEN=123456:ABC-DEF...

# URL бэкенда сайта (для бота). На том же сервере: http://localhost:3000
# API_BASE_URL=http://localhost:3000

# Базовый URL сайта (для ссылок регистрации и сброса пароля в боте). Продакшен: https://clabx.ru
# SITE_BASE_URL=https://clabx.ru

# Внешний ИИ (OpenAI/Claude): ключи можно задать здесь или в админ-панели (Внешний ИИ).
# OPENAI_API_KEY=sk-proj-...
# ANTHROPIC_API_KEY=sk-ant-...

# Шифрование API-ключей, сохранённых в админке (рекомендуется для продакшена).
# Задайте произвольную строку — ключи в БД будут храниться в зашифрованном виде (AES-256-GCM).
# ENCRYPTION_KEY=your_secret_encryption_key
